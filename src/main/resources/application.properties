spring.application.name=restaurant

spring.elasticsearch.uris=http://localhost:9200

# JWT Configuration
# Keycloak JWT issuer URI - matches your docker-compose.yaml configuration
# Keycloak admin: admin/admin, accessible at http://localhost:9090
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9090/realms/master
